# The public Auth0 MCP rollout currently supports ChatGPT and Codex.
# Hermes Agent should keep using the native life_archive memory provider until
# its remote OAuth client flow is implemented and verified separately.
mcp_servers: {}
